# Tauri 功能补全进度

## 已实现

- 子进程 stdout/stderr 捕获、后台任务日志、命令超时。
- PATH 排除 WindowsApps 占位程序，读取已保存的 Conda 可执行路径。
- 概览传播 Conda 查询错误，前端保留字符串错误详情。
- 并发操作 busy 计数、设置保存串行化。
- Windows venv 包管理解释器修正，pip show/uninstall 不再附加下载源。
- uv 元数据解析和卸载参数修正。
- venv 创建目录冲突检查、目录登记、删除前环境标识检查。
- 自定义 Miniconda 安装完成后保存实际可执行路径。
- Python 升级 dry-run、YAML 备份、版本校验。
- Conda Python 版本查询容忍诊断输出，Conda 导入使用非交互模式。
- Conda 核心升级增加 dry-run、base YAML 备份和前后版本记录。
- 设置页支持保存自定义 Conda 可执行文件路径。
- venv 面板支持检测 uv，并通过官方安装脚本一键安装 uv；安装过程显示后台任务和错误日志。
- 新增独立 uv 管理页：检测版本/路径、最新版或指定版本安装/更新、用户级 uv 卸载。
- 初始化配置恢复常用 Conda/pip 库勾选、Miniconda 维护信息、执行步骤和 Python 版本下拉框，默认 3.14。

## 验证

- Rust 实机只读测试已发现 base、paddleocr、py3146、py3147、trend-forecasting-py。
- 已增加子进程双输出流、失败命令、venv 解释器路径回归测试。
- Vue 生产构建通过。
- Tauri release 构建通过，已生成安装版和便携版。
- uv 安装命令、路径回退查找和创建环境前置检查已接入。

## 待完成（不能据此宣称完整发布）

- 扫描路径覆盖和 uv 完整能力适配仍需更多平台实测；当前一键安装需要联网。
- Conda 创建的复杂 pip 回退链路仍需端到端验证。
- Conda 核心升级的权限失败恢复仍需在受限目录实测。
- 独立临时环境的创建、克隆、导入导出、包操作端到端测试。
- 桌面 UI 的交互验收及发布文档同步。

当前没有执行业务环境升级或删除；本轮已生成 3.0.0 安装版和便携版，桌面 UI 仍需在实际运行窗口中验收。
